<p>Every Page will have this - this is index</p>
{{#if userPetData}}
  {{> usersPet/userPet-block }}
{{/if}}
{{#if pet}}
  {{> pets/pet-block }}
{{/if}}

{{#if user}}
  {{> users/user-block }}
{{/if}}

